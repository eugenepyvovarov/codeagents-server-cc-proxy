# chmod 600 /etc/claude-proxy.env
#
# Pick ONE authentication approach:
#
# 1) API key:
# ANTHROPIC_API_KEY=sk-ant-...
#
# 2) OAuth-style token (if your Claude Code install uses it):
# CLAUDE_CODE_OAUTH_TOKEN=...

# Push notifications (optional; managed by the iOS app when enabled):
# CODEAGENTS_PUSH_SECRET=...
# CODEAGENTS_PUSH_GATEWAY_BASE_URL=https://us-central1-<firebase-project-id>.cloudfunctions.net
